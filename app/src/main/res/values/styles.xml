<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="text_country_name">
        <item name="android:textSize">@dimen/text_country_details_name_size</item>
        <item name="android:textColor">@color/colorBlackText</item>
    </style>

    <style name="text_detail_main">
        <item name="android:textSize">@dimen/text_country_details_main_size</item>
        <item name="android:textColor">@color/colorBlackText</item>
        <item name="android:layout_marginTop">@dimen/delimiter_margin</item>
    </style>

</resources>
